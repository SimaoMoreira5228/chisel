# Minimal Chisel example configuration (sanitized)

[site]
name = "Example Site"
base_url = "http://localhost"
description = "A minimal, sanitized example for Chisel SSG"
author = "example"
language = "en"

[build]
output_dir = "dist"
content_dir = "content"
templates_dir = "templates"

[dev]
port = 3000
host = "localhost"